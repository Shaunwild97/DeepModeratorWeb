html
    head
        title DeepModerator's Server List
        style.
            body {
                font-family: 'Courier';
            }
            ul {
                list-style-type: none;
            }

            li {
                margin: 5px;
                display: inline-block;
            }

            a {
                padding: 5px;
                background: #DDDDDD;
                border-radius: 2px;
                display: inline-block;
                color: black;
                text-decoration: none;
            }

            a:hover {
                color: #AAAAAA;
            }
    body
        h1 #{count} Servers
        ul
            each val in servers
                li
                    a(href=`/server/${val.serverId}`)= val.serverName
